import { LockKeyhole, MapPin, Shield, Sparkles } from "lucide-react";
import { PageIntro, RoyalShell } from "@/components/royal-shell";

type Entry = { title: string; subtitle: string; detail: string; locked?: boolean };

export function LorePage({ eyebrow, title, description, entries }: { eyebrow: string; title: string; description: string; entries: Entry[] }) {
  return <RoyalShell><PageIntro eyebrow={eyebrow} title={title}>{description}</PageIntro><section className="px-5 py-10 sm:px-8 sm:py-12 md:px-10 lg:px-14 lg:py-14"><div className="mx-auto grid max-w-6xl gap-px border border-gold/25 bg-gold/25 lg:grid-cols-2">{entries.map((entry, index) => <article key={entry.title} className="group relative min-h-56 bg-paper p-6 sm:p-8 lg:min-h-64 lg:p-10"><span className="font-mono text-[10px] text-cinnabar">HỒ SƠ {String(index + 1).padStart(2, "0")}</span><div className="absolute right-6 top-6 text-gold/50 sm:right-8 sm:top-8">{entry.locked ? <LockKeyhole /> : index % 3 === 0 ? <MapPin /> : index % 3 === 1 ? <Shield /> : <Sparkles />}</div><h2 className="mt-7 break-words font-display text-2xl transition-colors group-hover:text-cinnabar sm:mt-8 sm:text-3xl">{entry.title}</h2><p className="mt-2 break-words text-sm font-semibold text-gold">{entry.subtitle}</p><p className="mt-4 max-w-lg break-words text-sm leading-7 text-ink/60 sm:mt-5 sm:text-base">{entry.detail}</p>{entry.locked && <p className="mt-6 border-t border-ink/10 pt-4 font-mono text-[10px] uppercase leading-5 text-ink/40 sm:mt-7">Chưa đủ điều kiện mở khóa</p>}</article>)}</div></section></RoyalShell>;
}